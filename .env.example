# =============================================================================
# RAG Agent Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Provider Configuration
# -----------------------------------------------------------------------------
# Provider: 'openai' | 'anthropic' | 'azure_openai'
PROVIDER=openai

# Model identifier (e.g., 'gpt-4o-mini', 'claude-3-5-sonnet-20241022')
MODEL=gpt-4o-mini

# Tool schema format: 'openai' | 'anthropic'
TOOL_FORMAT=openai

# -----------------------------------------------------------------------------
# OpenAI Configuration
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your-openai-api-key-here

# -----------------------------------------------------------------------------
# Anthropic Configuration
# -----------------------------------------------------------------------------
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# -----------------------------------------------------------------------------
# Azure OpenAI Configuration
# -----------------------------------------------------------------------------
# AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_DEPLOYMENT=your-deployment-name

# -----------------------------------------------------------------------------
# Vector Store Configuration
# -----------------------------------------------------------------------------
# Vector store type (currently only 'chroma' is supported)
VECTOR_STORE=chroma

# Chroma server configuration
CHROMA_HOST=localhost
CHROMA_PORT=8000

# Collection names (comma-separated for multiple collections)
# Examples:
#   Single: guidewire-code
#   Multiple: guidewire-code,templates,external-libs
CHROMA_COLLECTIONS=guidewire-code

# Optional: Chroma tenant and database (for multi-tenant deployments)
# CHROMA_TENANT=default_tenant
# CHROMA_DATABASE=default_database

# -----------------------------------------------------------------------------
# Agent Runtime Configuration
# -----------------------------------------------------------------------------
# Maximum number of tool calls per step
MAX_TURNS=10

# Default number of results from semantic search
TOP_K=6

# Logging level: 'error' | 'warn' | 'info' | 'debug' | 'trace'
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Agent System Prompt
# -----------------------------------------------------------------------------
# Path to the agent system prompt markdown file
# Can be absolute or relative to project root
AGENT_SYSTEM_PROMPT_PATH=./AGENT_SYSTEM_PROMPT.md

# Alternative prompt files for different environments:
# AGENT_SYSTEM_PROMPT_PATH=./prompts/production-agent.md
# AGENT_SYSTEM_PROMPT_PATH=./prompts/dev-agent.md

# -----------------------------------------------------------------------------
# Optional: Prompt Overrides
# -----------------------------------------------------------------------------
# You can override individual prompts by specifying them here.
# Leave commented to use defaults from src/config/prompts.ts

# PROMPT_PLANNER_SYSTEM="Your custom planner system prompt..."
# PROMPT_STEP_SYSTEM="Your custom step runner system prompt..."
# PROMPT_STEP_DEVELOPER="Your custom step runner developer message..."
# PROMPT_EVALUATOR_SYSTEM="Your custom evaluator system prompt..."
# PROMPT_FINALIZER_SYSTEM="Your custom finalizer system prompt..."

# =============================================================================
# End of Configuration
# =============================================================================
